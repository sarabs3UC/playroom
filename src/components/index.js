const React = require('react');
import { Button } from '@bit/sarabs3uc.brewbroker_ui.button'

module.exports = {
    Button: ({children}) => <button>{children}</button>,
    Header: ({}) => <div>It's a Header</div>
};